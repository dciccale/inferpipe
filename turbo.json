{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["APP_ORIGIN"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["APP_ORIGIN"]
    },
    "lint": {},
    "clean": {
      "cache": false
    }
  }
}
