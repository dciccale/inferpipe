{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["APP_ORIGIN", "CLERK_SECRET_KEY"]
    },
    "@inferpipe/ui#build": {
      "outputs": []
    },
    "@inferpipe/backend#build": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["APP_ORIGIN", "CLERK_SECRET_KEY"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "clean": {
      "cache": false
    }
  }
}
